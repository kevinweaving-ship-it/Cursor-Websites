# Event Series Candidates

Recurring events (same `event_name` across multiple years) → future **`/events/series/{slug}`**.

Group by `event_name`; for each series list:

- **event_name**
- **years** (array of years)
- **count** (occurrences)
- **example regatta_id** (one linked regatta for the series, if any)

**Examples expected:**
- HYC Cape Classic
- SA Youth Nationals
- Western Cape Champs

| event_name | years | count | example regatta_id |
|------------|-------|-------|---------------------|
| *(run script to populate)* | | | |
