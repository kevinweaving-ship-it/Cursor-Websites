# Compress JS/JSON/CSS including proxied API responses (nginx.conf has gzip on but only for text/html).
gzip_types text/plain text/css application/json application/javascript text/javascript application/xml image/svg+xml;
gzip_proxied any;
gzip_vary on;
gzip_min_length 1024;
gzip_comp_level 5;
