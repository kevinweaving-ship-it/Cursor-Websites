# LIVE NOW = 1 but empty Live list

Staff signed-in was counted in LIVE NOW (page hits) but filtered out of the
Live list (`staff IPs stay out of public Live`). Card and list now match:
signed-in staff stay visible; LIVE NOW uses `human_live` from `/live`.
