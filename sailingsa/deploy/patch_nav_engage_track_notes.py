# Notes: nav engage tracking

Applied live via `patch_nav_engage_track.py`.

Verified: `/auth/session?path=/events` then `/clubs` (4s gap) → previous hit
engagement = `clicked,scrolled`.

See `TRAFFIC_NAV_ENGAGE.md`.
